{
  "name": "spatial-sol",
  "version": "0.0.1",
  "description": "> TODO: description",
  "author": "Astral <contact@astral.global>",
  "homepage": "https://github.com/AstralProtocol/spatial-sol#readme",
  "license": "ISC",
  "main": "truffle-config.js",
  "directories": {
    "build": "build",
    "contracts": "contracts",
    "migrations": "migrations"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "tsc": "tsc",
    "test": "echo \"Error: run tests from root\" && exit 1",
    "console": "truffle console --network development",
    "ganache": "ganache-cli -h 0.0.0.0 -p 8545 -d -i 333",
    "truffle-build": "truffle compile -all",
    "truffle": "truffle compile --all && truffle migrate --network development --reset",
    "truffle-ropsten": "truffle compile --all && truffle migrate --network ropsten --reset && cp -vr ./build/contracts/SpatialAssets.json ../subgraph/abis/SpatialAssets.json",
    "mythx": "truffle run verify",
    "slither": "slither .",
    "truffle-test": "truffle test",
    "coverage": "truffle run coverage --solcoverjs ./.solcover.js --network development"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AstralProtocol/spatial-sol.git"
  },
  "bugs": {
    "url": "https://github.com/AstralProtocol/spatial-sol/issues",
    "access": "public"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^3.3.0",
    "@truffle/hdwallet-provider": "^1.2.1",
    "@turf/turf": "^5.1.6",
    "dotenv": "^8.2.0",
    "truffle": "^5.1.62"
  }
}
